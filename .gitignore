node_modules/
*.log
.env
.env.*
dist/
coverage/
.DS_Store
/tmp/
*.tsbuildinfo

# Vendored, built @aion/core (bootstrapped via scripts/setup-core.sh)
.vendor/

# Real call session records (PII) — never commit
data/
